name: test with dune pkg

on:
  push:
    branches: [ "master" ]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    strategy:
      matrix:
        runs-on: [ ubuntu-latest, macos-latest ]
    runs-on: ${{ matrix.runs-on }}
    steps:
    - name: Check out sources
      uses: actions/checkout@v6
    - name: Install and test
      uses: Leonidas-from-XIV/setup-dune@0173e38bffca83a844262acb2d8ac34889798289
